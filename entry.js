// Adding to the window object makes these modules available elsewhere, including inside the Alpine component!
// Global variables are generally frowned upon of course, but this seems to be the best viable solution
// e.g:
// window.simpleEncryptor = require('simple-encryptor')('this is a key that should be replaced by an ENV variable');
